# 1st_repo
# C# Array Operations Project

## Introduction
This repository demonstrates fundamental operations on **Arrays** in C#. An array is a data structure that stores a fixed-size sequential collection of elements of the same type. It is used to store a collection of data, but it is often more useful to think of an array as a collection of variables of the same type.

## Included Problems

### 1. Find Maximum Element
* **File:** `FindMax.cs`
* **Description:** This program iterates through an integer array to identify and print the largest value.
* **Key Concept:** Linear Search algorithm.

### 2. Reverse an Array
* **File:** `ReverseArray.cs`
* **Description:** This script takes an existing array and reverses the order of its elements (first becomes last, last becomes first).
* **Key Concept:** Using the `Array.Reverse()` built-in method.

### 3. Calculate Average
* **File:** `CalculateAverage.cs`
* **Description:** A program that sums all integers in an array and divides by the total count to find the mathematical average.
* **Key Concept:** Accumulators and Type Casting.

## How to Run
To run these files, you can use the .NET CLI:
```bash
dotnet run
